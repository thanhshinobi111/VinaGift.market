// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  root: 'VinaGift', // Thư mục gốc là VinaGift
  publicDir: 'VinaGift', // Sao chép các file tĩnh từ VinaGift
  build: {
    outDir: 'dist', // Xuất file build vào thư mục dist
    emptyOutDir: true, // Xóa thư mục dist trước khi build
    rollupOptions: {
      input: 'miniapp.html' // Entry point là miniapp.html (tương đối từ root)
    }
  },
  // Cấu hình để tương thích với Netlify
  base: '/', // Đường dẫn cơ sở cho tài nguyên
});